# دليل البدء السريع - تشغيل التطبيق على Windows

## ✅ الخطوات السريعة (5 دقائق)

### 1️⃣ تثبيت المتطلبات الأساسية

```powershell
# تثبيت Node.js (إذا لم يكن مثبتاً)
# قم بتنزيل من: https://nodejs.org/
# ثم تحقق من التثبيت:
node --version
npm --version

# تثبيت pnpm
npm install -g pnpm

# تثبيت Expo CLI
npm install -g expo-cli
```

### 2️⃣ استنساخ المشروع

```powershell
# انسخ المشروع
git clone <repository-url>
cd car_wash_inventory

# أو إذا كان لديك المشروع بالفعل
cd path\to\car_wash_inventory
```

### 3️⃣ تثبيت الاعتماديات

```powershell
pnpm install
```

### 4️⃣ تشغيل التطبيق

#### الطريقة الأولى: Expo Go (الأسهل والأسرع) ⭐

```powershell
# ابدأ خادم Expo
pnpm dev

# سيظهر رمز QR في الطرفية
# افتح تطبيق Expo Go على هاتفك الذكي
# امسح رمز QR
```

**المميزات:**
- ✅ لا تحتاج إلى Android Studio
- ✅ تحديثات فورية
- ✅ اختبار سريع جداً

#### الطريقة الثانية: Android Emulator

```powershell
# تأكد من تشغيل Android Emulator أولاً
# (افتح Android Studio → Device Manager)

# ثم شغل التطبيق
pnpm android
```

---

## 🔧 استكشاف الأخطاء الشائعة

### ❌ خطأ: 'expo' is not recognized

```powershell
# الحل 1: تثبيت Expo CLI عالمياً
npm install -g expo-cli

# الحل 2: استخدام npx
npx expo start --android

# الحل 3: استخدام pnpm
pnpm add -g expo-cli
```

### ❌ خطأ: 'pnpm' is not recognized

```powershell
# تثبيت pnpm
npm install -g pnpm

# تحقق من التثبيت
pnpm --version
```

### ❌ خطأ: المنفذ 8081 مشغول

```powershell
# استخدم منفذ مختلف
$env:EXPO_PORT=8082
pnpm dev

# أو أغلق العملية المستخدمة
# افتح Task Manager وأغلق العملية المتعلقة بـ node
```

### ❌ خطأ: لا يوجد اتصال بالإنترنت

```powershell
# تأكد من الاتصال بالإنترنت
# تأكد من أن الهاتف والكمبيوتر على نفس الشبكة
# جرب إعادة تشغيل الخادم
```

---

## 📱 تشغيل على هاتفك الذكي

### الخطوة 1: تثبيت Expo Go
- **iOS:** ابحث عن "Expo Go" في App Store
- **Android:** ابحث عن "Expo Go" في Google Play

### الخطوة 2: ابدأ الخادم
```powershell
pnpm dev
```

### الخطوة 3: امسح رمز QR
- افتح Expo Go على هاتفك
- اضغط على "Scan QR code"
- امسح الرمز الذي يظهر في الطرفية

### النتيجة
- التطبيق سيحمل على هاتفك
- يمكنك الآن اختباره بشكل كامل

---

## 🎯 الأوامر المهمة

```powershell
# تشغيل التطبيق
pnpm dev              # تطوير عام (Expo Go)
pnpm android          # تشغيل على Android Emulator
pnpm ios              # تشغيل على iOS Simulator (Mac فقط)

# الاختبار
pnpm test             # تشغيل جميع الاختبارات
pnpm test:watch       # مراقبة الاختبارات

# التحقق
pnpm check            # التحقق من TypeScript
pnpm lint             # التحقق من الأسلوب

# التنظيف
pnpm install --force  # إعادة تثبيت الاعتماديات
```

---

## 📋 قائمة التحقق

قبل البدء، تأكد من:

- [ ] Node.js مثبت (v16 أو أحدث)
- [ ] pnpm مثبت
- [ ] Expo CLI مثبت
- [ ] المشروع تم استنساخه
- [ ] `pnpm install` تم تنفيذه بنجاح
- [ ] الاتصال بالإنترنت يعمل
- [ ] Expo Go مثبت على هاتفك (اختياري)

---

## 🚀 الخطوات التالية

بعد تشغيل التطبيق:

1. **اختبر الميزات:**
   - تسجيل الدخول
   - إدارة المخزون
   - عرض التقارير

2. **استكشف الواجهة:**
   - تصفح الشاشات المختلفة
   - اختبر الملاحة

3. **اختبر الأداء:**
   - لاحظ سرعة التحميل
   - تحقق من استجابة الواجهة

---

## 💡 نصائح مفيدة

### تحديثات فورية
- عدّل ملف TypeScript/JavaScript
- اضغط `r` في الطرفية لإعادة تحميل
- التطبيق سيحدث فوراً على هاتفك

### عرض السجلات
- اضغط `j` في الطرفية لفتح قائمة الخيارات
- اضغط `l` لعرض السجلات

### إعادة تشغيل كاملة
- اضغط `Ctrl + C` في الطرفية
- ثم شغل `pnpm dev` مرة أخرى

---

## ❓ أسئلة شائعة

**س: هل أحتاج إلى Android Studio؟**  
ج: لا، يمكنك استخدام Expo Go على هاتفك مباشرة.

**س: هل يعمل على iPhone؟**  
ج: نعم، استخدم Expo Go من App Store.

**س: كيف أشارك التطبيق مع الآخرين؟**  
ج: شارك رابط Expo Go الذي يظهر في الطرفية.

**س: هل يمكن بناء APK؟**  
ج: نعم، استخدم `eas build --platform android --profile preview`

---

## 🆘 الحصول على المساعدة

إذا واجهت مشاكل:

1. **اقرأ الرسالة بعناية** - غالباً تحتوي على الحل
2. **تحقق من السجلات** - اضغط `l` في الطرفية
3. **ابحث عن الحل** - https://docs.expo.dev/
4. **اطلب المساعدة** - اتصل بفريق التطوير

---

**آخر تحديث:** 2026-04-14  
**الإصدار:** 1.0.0  
**الحالة:** جاهز للاستخدام ✅
